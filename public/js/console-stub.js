/**
 * Console stub - loaded first in <head> to avoid 404 and optional production silencing.
 * No-op; real console is left intact. Remove this file reference from HTML if not needed.
 */
(function () {
  'use strict';
  // Optionally silence console in production (uncomment to enable):
  // if (typeof window !== 'undefined' && /^\s*production\s*$/i.test(String((window.APP_ENV || window.NODE_ENV || '')))) {
  //   ['log','info','warn','debug'].forEach(function (m) { window.console[m] = function () {}; });
  // }
})();
